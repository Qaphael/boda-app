# Shape Migration + M3 Component Upgrades

Replace all 67 inline `RoundedCornerShape` declarations with `MaterialTheme.shapes.*` tokens. Upgrade 3 UI patterns to standard M3 components (SegmentedButton, FilterChip).

## Step 0: Update `ui/theme/Shape.kt`

Current Shape.kt has `large = topStart=28dp`. Update to `topStart=24dp` to match home card:

| Token | Value | Used for |
|-------|-------|----------|
| extraSmall | 4.dp | Chips, small badges, progress bars |
| small | 8.dp | Text fields, input areas, radio rows |
| medium | 16.dp | Cards, dialogs, chat bubbles |
| large | topStart=24.dp, topEnd=24.dp | Bottom sheets, home card |
| extraLarge | 28.dp | Full-screen modals |

## Step 1: Shape mapping rules

| Current code | → Token |
|-------------|---------|
| RoundedCornerShape(4.dp) | `MaterialTheme.shapes.extraSmall` |
| RoundedCornerShape(6.dp) | `MaterialTheme.shapes.extraSmall` |
| RoundedCornerShape(8.dp) | `MaterialTheme.shapes.small` |
| RoundedCornerShape(10.dp) | `MaterialTheme.shapes.small` |
| RoundedCornerShape(12.dp) | `MaterialTheme.shapes.small` |
| RoundedCornerShape(16.dp) | `MaterialTheme.shapes.medium` |
| RoundedCornerShape(20.dp) | `MaterialTheme.shapes.medium` |
| RoundedCornerShape(24.dp) | `MaterialTheme.shapes.medium` |
| RoundedCornerShape(topStart=24.dp, topEnd=24.dp) | `MaterialTheme.shapes.large` |
| RoundedCornerShape(topStart=16.dp, topEnd=16.dp) | `MaterialTheme.shapes.large` |
| RoundedCornerShape(topStart=20.dp, topEnd=20.dp) | `MaterialTheme.shapes.medium` |
| RoundedCornerShape(28.dp) | `MaterialTheme.shapes.extraLarge` |
| CircleShape | **keep as-is** |
| RoundedCornerShape(2.dp), RoundedCornerShape(3.dp) | **keep as-is** (too small for scale) |
| Multi-radius: topStart=X, topEnd=Y, bottomStart=A, bottomEnd=B | **keep as-is** (asymmetric) |

## Step 2: Component upgrades

### HomeScreen.kt — Ride/Delivery toggle → SegmentedButton

Replace the two Box composables with `SingleChoiceSegmentedButtonRow` + `SegmentedButton`.

### HomeScreen.kt — Saved places chips → FilterChip

Replace Box + clickable + border pattern with `FilterChip`.

### WalletScreen.kt — Top-up amount chips → FilterChip

Replace Box + clickable + clip pattern with `FilterChip`.

## Step 3: Files to edit

| File | Occurrences | Notes |
|------|-------------|-------|
| `ui/home/HomeScreen.kt` | 12 | SegmentedButton + FilterChip upgrades |
| `ui/wallet/WalletScreen.kt` | 1 | FilterChip upgrade |
| `ui/ride/ActiveTripScreen.kt` | 4 | Shape tokens only |
| `ui/ride/RoutePreviewScreen.kt` | 1 | Shape token only |
| `ui/ride/SearchPlacesScreen.kt` | 1 | Shape token only |
| `ui/ride/RiderEnRouteScreen.kt` | 1 | Shape token only |
| `ui/chat/RiderChatOverlay.kt` | 5 | Shape tokens (keep asymmetric chat bubble) |
| `ui/auth/SplashScreen.kt` | 1 | Shape token |
| `ui/auth/OnboardingScreen.kt` | 6 | Keep 2.dp drag handle |
| `ui/driver/DriverOnboardingScreen.kt` | 7 | Keep 3.dp progress bar |
| `ui/referrals/ReferralsScreen.kt` | 5 | Shape tokens |
| `ui/profile/ProfileSettingsScreen.kt` | 5 | Shape tokens |
| `ui/profile/SupportScreen.kt` | 3 | Shape tokens |
| `ui/trips/TripsHistoryScreen.kt` | 1 | Shape token |
| `ui/offline/OfflineSMSBookingOverlay.kt` | 2 | Shape tokens |

**Total: ~67 replacements + 3 component upgrades across 15 files**

## Step 4: Commit

Message: `refactor: replace inline RoundedCornerShape with MaterialTheme.shapes tokens`

## Step 5: Verification

- `./gradlew.bat assembleDebug` — user verifies in Android Studio
- Visual check: all cards have consistent corner radius
- Ride/Delivery toggle now uses SegmentedButton (M3 standard)
- Saved places and wallet chips use FilterChip (M3 standard)
